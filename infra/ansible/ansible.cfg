[defaults]
inventory = ./inventory/production.ini

roles_path = ./roles

remote_user = gateway-deploy

host_key_checking = False

stdout_callback = yaml

forks = 5

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False

[ssh_connection]
pipelining = True