// Docker registry Settings
DOCKER_REGISTRY = "registry.digital-oriental-gateway.com"
DOCKER_CREDS_ID = "srm-registry-credentials"

// Image Names (Matching docker-compose services)
IMAGE_API = "srm-api"
IMAGE_UI = "srm-ui"
IMAGE_WORKER = "srm-worker"

// Project Paths
PATH_API_PROJ = "src/backend/Srm.Gateway.Api/Srm.Gateway.Api.csproj" // Corrigé: backend (pas bacend)
PATH_UI_DIR   = "src/frontend/srm-dashboard"
PATH_WORKER_DIR = "src/workers/ocr-service"
PATH_DOCKER   = "infra/docker"

// Tooling Versions
TOOL_DOTNET = "dotnet-9"
TOOL_NODE = "node-20"

return this